{
  "name": "snapsell-app",
  "version": "1.0.0",
  "description": "SnapSell - Ürün görseli ve liste metni oluşturma",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node start.js",
    "dev": "node server.js",
    "build": "npm run build:design",
    "deploy:firebase": "npm run build && echo. && echo [KURULUM] Firebase icin dist/config.json apiUrl gerekirse ayarlayin, sonra: firebase deploy && echo.",
    "build:css": "npx tailwindcss -i ./src/input.css -o ./public/css/tailwind.css --minify",
    "build:design": "npm run build --prefix saas-design-extracted",
    "postinstall": "node -e \"try { require('fs').mkdirSync('data', { recursive: true }); } catch (e) {}\"",
    "load-plans": "node scripts/load-plans.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.15"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.98.0",
    "axios": "^1.13.6",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "firebase-admin": "^12.7.0",
    "form-data": "^4.0.5",
    "node-fetch": "^2.7.0",
    "openai": "^6.22.0",
    "sharp": "^0.33.5"
  }
}
